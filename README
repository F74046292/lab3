	Question A:
		
	g++ -o main main.cpp

	Question B: 
	 size = 1000
        output:
            sort(): 0 seconds
            v1/v2 are different.
            insertion_sort(): 0.02 seconds
            v1/v2 are the same.

     size = 10000
        output:
            sort(): 0 seconds   
            v1/v2 are different.
            insertion_sort(): 1.31 seconds
            v1/v2 are the same.

     size = 100000
        output:
            sort(): 0.04 seconds
            v1/v2 are different.
            insertion_sort(): 127.32 seconds
            v1/v2 are the same.

     size = 1000000
        output:
            sort(): 0.54 seconds
            v1/v2 are different.
            insertion_sort(): 12313.2 seconds
            v1/v2 are the same.

	
